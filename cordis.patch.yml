# anime-wallpaper skin bundle patch: inserts its public dsh.client entry into
# the web plugin roster. Install with `dsh plugin --profile <name> add <path>`.
- insert:
    - id: ui-skin-anime-wallpaper
      name: '@dsh-external/dsh-client-ui-skin-anime-wallpaper'
