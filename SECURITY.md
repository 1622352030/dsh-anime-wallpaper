# 安全政策

## 报告安全漏洞

如果你发现了安全漏洞，请通过 [GitHub Issue](https://github.com/1622352030/dsh-anime-wallpaper/issues) 报告，并在报告中尽量详细地描述问题。

请不要在公开场合（如评论、社交平台）提前披露尚未修复的漏洞细节，给维护者留出处理时间。

## 范围说明

本项目是一个**纯展示层的客户端皮肤插件**：

- 不注入任何服务；
- 不发出 Cordis 事件；
- 不触达模型请求；
- 不访问文件系统或网络（除浏览器本身加载 bundle 外）；
- 所有数据（壁纸选择、自定义图、重命名记录）仅存储在浏览器 localStorage。

因此它的安全影响面很小。尽管如此，如果发现任何问题——包括但不限于：

- 恶意构造的文件名/图片导致异常
- localStorage 数据被意外篡改或泄露
- 与其他插件的交互产生意外行为

都欢迎报告。

## 支持的版本

目前仅维护 `main` 分支的最新版本。

## 处理流程

1. 收到报告后，维护者会尽快确认问题。
2. 修复会随下一次提交进入 `main` 分支。
3. 修复后会在对应 Issue 中说明。
